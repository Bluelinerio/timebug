import { lensProp } from 'ramda'

export const user = lensProp('user')
export const cms = lensProp('cms')
export const formData = lensProp('formData')
export const agregates = lensProp('agregates')
export const error = lensProp('error')
export const network = lensProp('network')
export const appState = lensProp('appState')