//@flow
import {
  UNDETERMINED
} from '../../types'

export const initialState = UNDETERMINED
const notificationsReducer = (state = initialState, action) => {
  
}

export default notificationsReducer