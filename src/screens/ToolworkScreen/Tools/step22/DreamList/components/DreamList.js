import React from 'react'
import { ScrollView, Text } from 'react-native'

const DreamList = () => {
  return (
    <ScrollView>
      <Text>DreamList</Text>
    </ScrollView>
  )
}

export default DreamList
