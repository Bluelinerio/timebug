import React from 'react'
import { ScrollView, Text } from 'react-native'

const FavoritesList = () => {
  return (
    <ScrollView>
      <Text>Favorites</Text>
    </ScrollView>
  )
}

export default FavoritesList
