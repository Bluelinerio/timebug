import React                                   from 'react'
import { View, Text }              from 'react-native'

const screen = (props) => (
    <View>
        <Text>
            MyJourney
        </Text>
    </View>
)

export default screen
