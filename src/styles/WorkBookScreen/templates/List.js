import theme                    from 'react-native-theme';
import { PixelRatio, Platform } from "react-native";


theme.add({
  customListAddButton: {
    width: 24,
    height: 24,
    padding: 3,
    borderRadius: 50,
    borderWidth: 1,
    borderColor: '#ccc'
  }
});
